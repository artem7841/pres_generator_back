version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - CONNECTION_STRING=Data/database.db
    volumes:
      - ./PresentattionApi/Data:/app/Data
    ports:
      - "5000:5000"
