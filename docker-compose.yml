version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env  
    volumes:
      - ./PresentationApi/Data:/app/Data
    ports:
      - "5204:5000"